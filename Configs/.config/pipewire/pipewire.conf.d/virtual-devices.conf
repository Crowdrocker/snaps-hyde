# Virtual devices for audio separation
context.modules = [
    { name = libpipewire-module-loopback
        args = {
            node.description = "Game Audio"
            capture.props = {
                node.name = "game_capture"
                media.class = "Audio/Sink"
            }
            playback.props = {
                node.name = "game_playback"
                media.class = "Audio/Source"
            }
        }
    }
    { name = libpipewire-module-loopback
        args = {
            node.description = "Discord Audio"
            capture.props = {
                node.name = "discord_capture"
                media.class = "Audio/Sink"
            }
            playback.props = {
                node.name = "discord_playback"
                media.class = "Audio/Source"
            }
        }
    }
]
